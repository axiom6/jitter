
class Choices

  Jitter.Choices = Choices

  constructor:( @stream ) ->

  overview:(   pane, spec ) ->
    Util.noop( pane, spec )
    return

  ready:(    pane, spec ) ->

    return

  create:(     pane, spec ) ->
    Util.noop( pane, spec )
    return